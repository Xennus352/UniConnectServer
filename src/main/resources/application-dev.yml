# ============================================================
# Development profile – activates dev/test seed data
# ============================================================
# Start with:  ./mvnw spring-boot:run -Dspring-boot.run.profiles=dev
# Or:          java -jar app.jar --spring.profiles.active=dev
# ============================================================

spring:
  datasource:
    # Points at the CURRENT (new) Neon project; DB_URL/DB_USERNAME/DB_PASSWORD
    # environment variables still take precedence for local overrides.
    url: ${DB_URL:jdbc:postgresql://ep-empty-leaf-azyy2750-pooler.c-3.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&connectTimeout=60&socketTimeout=120}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_PMrCxFm5s2If}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

logging:
  level:
    com.unicconnect: DEBUG
    org.springframework.security: DEBUG
